# Редактор статей - Инструкция

Веб-редактор для написания и редактирования статей блога прямо из браузера.

## Быстрый старт

1. Откройте `admin.html` в браузере
2. Введите GitHub Personal Access Token
3. Начните писать статьи!

## Настройка GitHub Personal Access Token

### Создание токена

1. Перейдите на GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Нажмите "Generate new token (classic)"
3. Установите права:
   - ✅ `repo` (полный доступ к репозиторию)
   - ✅ `workflow` (для GitHub Actions)
4. Скопируйте токен (он показывается только один раз!)

### Сохранение токена

- Введите токен в поле "GitHub Personal Access Token"
- Нажмите "Сохранить"
- Токен сохранится в localStorage браузера

## Использование редактора

### Создание новой статьи

1. Нажмите кнопку **"+ Новая"** в сайдбаре
2. Заполните метаданные:
   - **Заголовок** - обязательное поле
   - **Дата публикации** - по умолчанию сегодня
   - **Теги** - через запятую
   - **Краткое описание** - для списка статей
   - **Время чтения** - в минутах
   - **Статус** - черновик или опубликовано
   - **Иконка** - имя файла из `assets/icons/`
3. Напишите статью в редакторе
4. Нажмите **"💾 Сохранить в GitHub"**

### Редактирование статьи

1. Выберите статью из списка в сайдбаре
2. Статья загрузится в редактор
3. Внесите изменения
4. Нажмите **"💾 Сохранить в GitHub"**

### Режимы редактирования

- **Редактор** - WYSIWYG редактор (TipTap)
- **Markdown** - редактирование в формате Markdown
- **Предпросмотр** - просмотр готовой статьи

### Загрузка изображений

1. Перетащите изображение в область "📷 Перетащите изображение сюда"
2. Или нажмите на область и выберите файл
3. Изображение автоматически загрузится в `blog/images/<article-id>/`
4. Ссылка на изображение вставится в статью

## Автоматическая сборка

После сохранения статьи в GitHub:

1. GitHub Actions автоматически запускает сборку
2. Markdown конвертируется в HTML
3. HTML файлы сохраняются в `blog/posts/`
4. Сайт обновляется через GitHub Pages

## Формат Markdown

Редактор поддерживает:

- **Заголовки**: `# H1`, `## H2`, `### H3`
- **Жирный текст**: `**текст**`
- **Курсив**: `*текст*`
- **Ссылки**: `[текст](url)`
- **Изображения**: `![alt](path)` или `[IMAGE:path]`
- **Код**: `` `inline` `` или блоки с ` ``` `
- **Списки**: `- пункт` или `1. пункт`
- **Цитаты**: `> текст`
- **Сворачиваемые секции**:
  ```
  >>> Заголовок секции
  Контент секции
  <<<
  ```

## Структура файлов

- `blog/content/<id>.md` - исходные markdown файлы
- `blog/articles.json` - метаданные статей
- `blog/posts/<date>-<id>.html` - собранные HTML файлы (генерируются автоматически)
- `blog/images/<id>/` - изображения для статей

## Безопасность

- Токен хранится только в localStorage вашего браузера
- Никогда не коммитьте токен в репозиторий
- Используйте токен с минимально необходимыми правами
- Регулярно обновляйте токен

## Устранение проблем

### "Токен не установлен"
- Убедитесь, что ввели токен и нажали "Сохранить"
- Проверьте, что токен имеет права `repo`

### "Ошибка загрузки статей"
- Проверьте подключение к интернету
- Убедитесь, что токен действителен
- Проверьте права токена

### "Ошибка сохранения"
- Убедитесь, что заполнен заголовок статьи
- Проверьте, что токен имеет права на запись
- Проверьте консоль браузера для деталей ошибки

### Изображения не загружаются
- Убедитесь, что статья создана/открыта
- Проверьте размер файла (GitHub ограничивает до 100MB)
- Используйте форматы: .webp, .jpg, .png

## Дополнительно

- Автосохранение черновиков в localStorage
- Автоматический коммит в GitHub при сохранении
- Предпросмотр статьи в реальном времени
- Поддержка drag & drop для изображений

